name: DevxyzCrate v1.0 CapCut Reference Parity APK

on:
  push:
    branches: [ rebuild/devxyzcrate-v10-capcut-parity ]
  workflow_dispatch:

jobs:
  build-apk:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout bootstrap
        uses: actions/checkout@v4

      - name: Reconstruct v0.5 source
        shell: bash
        run: |
          cat .bootstrap_v05/chunk_*.b64 > DevxyzCrate-v05.b64
          base64 --decode DevxyzCrate-v05.b64 > DevxyzCrate-v05.zip
          mkdir -p source
          unzip -q DevxyzCrate-v05.zip -d source
          unzip -t DevxyzCrate-v05.zip
          test -f source/settings.gradle.kts
          test -f source/app/build.gradle.kts

      - name: Apply v0.6 smooth UX patch
        shell: bash
        run: |
          python .v06_ux_patch.py
          grep -q 'versionName = "0.6.0"' source/app/build.gradle.kts

      - name: Apply v0.7 reference UI patch
        shell: bash
        run: |
          cat .v07_patch_chunks/chunk_*.b64 | base64 --decode | gzip -dc > /tmp/v07.patch
          patch -p1 -d source < /tmp/v07.patch
          grep -q 'versionName = "0.7.0"' source/app/build.gradle.kts

      - name: Apply v0.8 functional editor patch
        shell: bash
        run: |
          cat .v08_patch_chunks/chunk_*.b64 | base64 --decode | gzip -dc > /tmp/v08.patch
          patch -p1 -d source < /tmp/v08.patch
          grep -q 'versionName = "0.8.0"' source/app/build.gradle.kts

      - name: Apply v0.9 smooth HD patch
        shell: bash
        run: |
          cat .v09_patch_chunks/chunk_*.b64 | base64 --decode | gzip -dc > /tmp/v09.patch
          patch -p1 -d source < /tmp/v09.patch
          grep -q 'versionName = "0.9.0"' source/app/build.gradle.kts

      - name: Apply v1.0 CapCut parity patch
        shell: bash
        run: |
          cat .v10_patch_chunks/chunk_*.b64 | base64 --decode | gzip -dc > /tmp/v10.patch
          patch -p1 -d source < /tmp/v10.patch
          grep -q 'versionName = "1.0.0"' source/app/build.gradle.kts
          grep -q 'NoiseGateAudioProcessor' source/app/src/main/java/com/jepongdevxyz/vidcrate/media/NoiseGateAudioProcessor.kt
          grep -q 'reduceNoise' source/app/src/main/java/com/jepongdevxyz/vidcrate/editor/EditorModels.kt
          grep -q 'CapExportSettingsScreen' source/app/src/main/java/com/jepongdevxyz/vidcrate/ui/EditorScreen.kt
          grep -q 'CapEffectsPanel' source/app/src/main/java/com/jepongdevxyz/vidcrate/ui/EditorScreen.kt
          grep -q 'ImportSource' source/app/src/main/java/com/jepongdevxyz/vidcrate/ui/ReferenceFlowScreens.kt
          grep -q 'snapshotFlow' source/app/src/main/java/com/jepongdevxyz/vidcrate/ui/EditorScreen.kt
          grep -q 'METADATA_KEY_VIDEO_WIDTH' source/app/src/main/java/com/jepongdevxyz/vidcrate/media/MediaProbe.kt
          grep -q 'DefaultEncoderFactory' source/app/src/main/java/com/jepongdevxyz/vidcrate/media/Media3ExportEngine.kt

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Install Android SDK packages
        shell: bash
        run: |
          export PATH="$ANDROID_SDK_ROOT/cmdline-tools/latest/bin:$ANDROID_SDK_ROOT/platform-tools:$PATH"
          yes | sdkmanager --licenses >/dev/null || true
          sdkmanager "platform-tools" "platforms;android-36" "build-tools;36.0.0"

      - name: Set up Gradle 9.6
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '9.6.0'

      - name: Build debug APK
        working-directory: source
        run: gradle :app:assembleDebug --stacktrace

      - name: Verify APK
        shell: bash
        run: |
          test -s source/app/build/outputs/apk/debug/app-debug.apk
          cp source/app/build/outputs/apk/debug/app-debug.apk DevxyzCrate-v1.0-capcut-parity-debug.apk
          unzip -t DevxyzCrate-v1.0-capcut-parity-debug.apk
          sha256sum DevxyzCrate-v1.0-capcut-parity-debug.apk
          ls -lh DevxyzCrate-v1.0-capcut-parity-debug.apk

      - name: Package source
        shell: bash
        run: |
          cd source
          zip -qr ../DevxyzCrate-v1.0-capcut-parity-source.zip . -x ".gradle/*" "app/build/*" "build/*"
          cd ..
          unzip -t DevxyzCrate-v1.0-capcut-parity-source.zip

      - name: Upload APK and source
        uses: actions/upload-artifact@v4
        with:
          name: DevxyzCrate-v1.0-capcut-parity
          path: |
            DevxyzCrate-v1.0-capcut-parity-debug.apk
            DevxyzCrate-v1.0-capcut-parity-source.zip
          if-no-files-found: error
