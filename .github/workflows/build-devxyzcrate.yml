name: DevxyzCrate v0.7 Reference UI APK

on:
  push:
    branches: [ rebuild/devxyzcrate-v07-reference ]
  workflow_dispatch:

jobs:
  build-apk:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout bootstrap
        uses: actions/checkout@v4

      - name: Reconstruct v0.5 source
        shell: bash
        run: |
          cat .bootstrap_v05/chunk_*.b64 > DevxyzCrate-v05.b64
          base64 --decode DevxyzCrate-v05.b64 > DevxyzCrate-v05.zip
          mkdir -p source
          unzip -q DevxyzCrate-v05.zip -d source
          unzip -t DevxyzCrate-v05.zip
          test -f source/settings.gradle.kts
          test -f source/app/build.gradle.kts

      - name: Apply v0.6 smooth UX patch
        shell: bash
        run: |
          python .v06_ux_patch.py
          grep -q 'versionName = "0.6.0"' source/app/build.gradle.kts
          grep -q 'detectTransformGestures' source/app/src/main/java/com/jepongdevxyz/vidcrate/ui/EditorScreen.kt

      - name: Apply v0.7 reference UI patch
        shell: bash
        run: |
          cat .v07_patch_chunks/chunk_*.b64 | base64 --decode | gzip -dc > /tmp/v07.patch
          patch -p1 -d source < /tmp/v07.patch
          grep -q 'versionName = "0.7.0"' source/app/build.gradle.kts
          test -f source/app/src/main/java/com/jepongdevxyz/vidcrate/ui/ReferenceFlowScreens.kt
          grep -q 'ReferenceMediaImportScreen' source/app/src/main/java/com/jepongdevxyz/vidcrate/ui/DevxyzCrateApp.kt
          grep -q 'AI UHD' source/app/src/main/java/com/jepongdevxyz/vidcrate/ui/EditorScreen.kt

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Install Android SDK packages
        shell: bash
        run: |
          export PATH="$ANDROID_SDK_ROOT/cmdline-tools/latest/bin:$ANDROID_SDK_ROOT/platform-tools:$PATH"
          yes | sdkmanager --licenses >/dev/null || true
          sdkmanager "platform-tools" "platforms;android-36" "build-tools;36.0.0"

      - name: Set up Gradle 9.6
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '9.6.0'

      - name: Build debug APK
        working-directory: source
        run: gradle :app:assembleDebug --stacktrace

      - name: Verify APK
        shell: bash
        run: |
          test -s source/app/build/outputs/apk/debug/app-debug.apk
          cp source/app/build/outputs/apk/debug/app-debug.apk DevxyzCrate-v0.7-reference-debug.apk
          unzip -t DevxyzCrate-v0.7-reference-debug.apk
          sha256sum DevxyzCrate-v0.7-reference-debug.apk
          ls -lh DevxyzCrate-v0.7-reference-debug.apk

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: DevxyzCrate-v0.7-reference-debug-apk
          path: DevxyzCrate-v0.7-reference-debug.apk
          if-no-files-found: error
